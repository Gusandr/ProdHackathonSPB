<resources>
    <style name="Theme.ProdHackathonSPB.Base" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/launch_background</item>
    </style>

    <!-- На старых версиях мапим основной стиль на Base -->
    <style name="Theme.ProdHackathonSPB" parent="Theme.ProdHackathonSPB.Base" />

    <style name="BottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/MyBottomSheetStyle</item>
    </style>

    <style name="MyBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>
